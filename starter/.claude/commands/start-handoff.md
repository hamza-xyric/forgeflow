---
description: Execute the kickoff prompt generated by /handoff in a fresh session
---

Read `.claude/plans/next-session-handoff.md` and treat its full contents as your kickoff prompt for this session.

That file was written by a previous session that had full working context of what was just completed — it contains the rehydration steps, the next concrete unit of work, decisions to flag, conventions to apply, and stop conditions.

Execute it exactly as written:
- Begin by running the **START BY:** verification steps at the top.
- Honor every **AskUserQuestion** checkpoint before any writes.
- Apply every rule in the **RULES STILL IN FORCE** block.
- Stop when any condition in **STOP CONDITIONS** is met.

If the handoff file does not exist or is empty, tell the user there's no active handoff and they should run `/handoff` first in their previous session.
